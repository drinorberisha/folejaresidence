/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['d124q86xsdw2f2.cloudfront.net','foleja-bucket.s3.eu-central-1.amazonaws.com'],
          },
};

export default nextConfig;
